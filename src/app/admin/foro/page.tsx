import { ForumReports } from "@/components/app/ForumReports";

export const metadata = { title: "Reportes del foro — Admin" };

export default function AdminForumPage() {
  return <ForumReports />;
}
